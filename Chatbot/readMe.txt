Dependencies:
pip install openai
pip install gradio
pip install nltk
pip install spacy
python -m spacy download en_core_web_sm

Run program: (make sure you're in the chatbot folder when doing this)
python3 hollowbot.py

Copy local url to browser while program is running to interact with the chatbot.
E.g. http://127.0.0.1:7860